services:
  postgres-cfelbd:
    image: postgres:16-alpine
    container_name: postgres-cfelbd
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    ports:
      - "5437:5432"
    volumes:
      - postgres-cfelbd-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - orghub-cfl-network
    restart: unless-stopped

  orghub-cfl-api:
    image: orghub-api:latest
    container_name: orghub-cfl-api
    environment:
      - ASPNETCORE_ENVIRONMENT=CFLLive
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__DefaultConnection=Host=postgres-cfelbd;Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
      - Jwt__Key=${JWT_KEY}
      - Jwt__Issuer=${JWT_ISSUER}
      - Jwt__Audience=${JWT_AUDIENCE}
    ports:
      - "127.0.0.1:5178:8080"
    depends_on:
      postgres-cfelbd:
        condition: service_healthy
    networks:
      - orghub-cfl-network
    restart: unless-stopped

volumes:
  postgres-cfelbd-data:

networks:
  orghub-cfl-network:
    driver: bridge
